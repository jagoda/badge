"use strict";
var Lab = require("lab");

var describe = Lab.describe;
var it       = Lab.it;

describe("A Logger", function () {
	describe("without any tags", function () {
		it("can create a formatted 'info' message");

		it("can create a formatted 'warn' message");

		it("can create a formatted 'error' message");
	});

	describe("with tags", function () {
		it("can create a formatted 'info' message");

		it("can create a formatted 'warn' message");

		it("can create a formatted 'error' message");
	});
});
